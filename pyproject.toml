[project]
name = "wechat-wmpf-crawler"
version = "0.1.0"
description = "Local WMPF CDP account collector with a bundled debugger runtime"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "psutil>=7.0",
    "pywin32>=311",
    "websockets>=15.0",
]

[tool.uv]
package = false
